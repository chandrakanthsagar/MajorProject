<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/styles.css" />
    <title>Sports Scheduler</title>
   <style>
            nav{
             display:flex;
             background-color: rgb(23, 30, 43);
             padding: 10px;
             justify-content: space-between;
        }
        nav button {
             margin: 0 10px; /* Add margin between buttons as needed */
             padding: 6px 24px; /* Add padding to buttons as needed */
             background-color: #3498db; /* Set the background color of the buttons */
             color: white; /* Set the text color of the buttons */
            border: none; /* Remove default button border */
            border-radius: 4px; /* Add border-radius for rounded corners */
            cursor: pointer; /* Change cursor to pointer on hover */   
            margin-left: 15px;
            margin-right: 15px;
        }
        .b{
            display: flex;
            justify-content: space-between ;
        }

        /* Additional styling for button hover effect */
        nav button:hover {
        background-color: #2980b9; /* Change background color on hover */
    }
   </style>
</head>

<body class="bg-blue-100">
  <nav>
    <div class="b">
        <button >Home</button>
        <button> <a href="/about">About</a></button>
    </div>
    <div>
        <h1 class="text-3xl mb-3 font-serif text-white">
            Sports Scheduler Web Application
          </h1>
          
    </div>
    <div class="b">
        <button>Contact</button>
       
    </div>
   
   
</nav>
        
        <a href="/signout" class="text-red-900 text-sm px-2 py-1 absolute top-2 right-2">Signout</a>
        <div class="container mx-auto px-10 py-4">
            <h1 class="text-6xl font-serif text-grey-600 mb-90">
              <%= sports.sportname %>!
            </h1>
          
            <h1 class="text-4xl font-serif text-blue-600 mb-90" >Upcoming sessions</h1>
            <% for (var i = 0; i < sessions.length; i++) { %>
              <% if (sessions[i].isCreated && sessions[i].date >= new Date()) { %>
                <a href="/createdsession/<%= sessions[i].id %>" class="inline-block px-4 py-2 border border-gray-300 bg-gray-100 text-gray-700 hover:bg-gray-200 hover:text-gray-900">
                  <%= sessions[i].date %>
                </a>
          <br>
                <br>
              <% } %>
              

            <% } %>
            <h1 class="text-4xl font-serif text-blue-600 mb-90">Past sessions </h1>
            <% for (var i = 0; i < sessions.length; i++) { %>
                
                <% if (sessions[i].isCreated && sessions[i].date < new Date()) { %>
                  <a href="/createdsession/<%= sessions[i].id %>" class="inline-block px-4 py-2 border border-gray-300 bg-gray-100 text-gray-700 hover:bg-gray-200 hover:text-gray-900">
                    <%= sessions[i].date %>
                  </a>
            <br>
                  <br>
                <% } %>
  
              <% } %>

          </div>
          
       
 </body>
</html>